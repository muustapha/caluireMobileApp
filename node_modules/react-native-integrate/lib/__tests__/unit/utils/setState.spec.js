"use strict";
/* eslint-disable @typescript-eslint/no-unsafe-call */
Object.defineProperty(exports, "__esModule", { value: true });
const setState_1 = require("../../../utils/setState");
const variables_1 = require("../../../variables");
describe('setState', () => {
    it('should not set state to done if it is not progress', () => {
        variables_1.variables.clear();
        variables_1.variables.set('test', {
            state: 'skipped',
        });
        (0, setState_1.setState)('test', {
            state: 'done',
            error: false,
        });
        expect(variables_1.variables.get('test')).toEqual({
            state: 'skipped',
        });
    });
});
