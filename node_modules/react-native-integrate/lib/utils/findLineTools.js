"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findLineEnd = exports.findLineStart = void 0;
function findLineStart(content, characterIndex, minIndex, includeLineBreak = false) {
    minIndex = Math.max(minIndex ?? 0, 0);
    let lineStart = characterIndex;
    while (lineStart > minIndex && content[lineStart] !== '\n') {
        lineStart--;
    }
    return !includeLineBreak && content[lineStart] === '\n'
        ? lineStart + 1
        : lineStart;
}
exports.findLineStart = findLineStart;
function findLineEnd(content, characterIndex, maxIndex) {
    maxIndex = Math.min(maxIndex ?? content.length - 1, content.length - 1);
    let lineStart = characterIndex;
    while (lineStart < maxIndex && content[lineStart] !== '\n') {
        lineStart++;
    }
    return lineStart;
}
exports.findLineEnd = findLineEnd;
