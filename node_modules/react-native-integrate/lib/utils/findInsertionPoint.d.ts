import { TextOrRegex } from '../types/mod.types';
export declare function findInsertionPoint(content: string, textOrRegex: TextOrRegex): {
    start: number;
    end: number;
    match: string | null;
};
